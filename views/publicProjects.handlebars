    <header>
        <div class = 'navbar-top'>
            <a href = '#' class = 'log-in-link'>Log In</a>
        </div>
    </header>

    <div class="public-projects-body container">
      <div class="row mt-5">
        {{#each project}}
        <div class="col-md-6">
          <div class="row">
            <div class="col-md-8" style="height: 300px; background-color: #868DE8">
              <img src="{{image}}" alt="">
            </div>
            <div class="col-md-4" style="height: 300px; background-color: #fff;">
              {{> projects/project-info-block}}
            </div>
          </div>
        </div>
        {{/each}}
    </div>